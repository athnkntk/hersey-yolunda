databases:
  - name: hersey-yolunda-db
    database: hersey
    user: hersey
    plan: free
    postgresMajorVersion: "17"

services:
  - type: web
    name: hersey-yolunda-api
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /v1/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: SCHEDULER_MODE
        value: external
      - key: DATABASE_URL
        fromDatabase:
          name: hersey-yolunda-db
          property: connectionString
      - key: DATA_ENCRYPTION_KEY
        sync: false
      - key: LOOKUP_KEY
        sync: false
      - key: TICK_SECRET
        sync: false
